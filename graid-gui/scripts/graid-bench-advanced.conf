# Graid Benchmark Advanced Configuration
# These parameters are less commonly used but available for fine-tuning.

# Enable scanning logic (default: false)
SCAN="false"

# FIO Job List (default: false)
LS_JB="false"

# FIO Block Size List (default: false)
LS_BS="false"

# Custom List (default: false)
LS_CUST="false"

# Custom Strip Size (default: false)
LS_CS="false"

# Wait for CPU Cool Down (default: false)
WCD="false"

# Dummy Run Mode (default: false)
DUMMY="false"

# Temperature Threshold for WCD (default: 75)
TEMP="75"

# CPU Affinity for Random Workloads (default: socket 0 + 1 cores logic, usually set in script)
# Uncomment to override defaults:
# CPU_ALLOWED_RAND="0,4,8,12,16,20,24,28,32,36,40,44,48,52,56,60,64,68,72,76,80,84,88,92,96,100,104,108,112,116,120,124"
